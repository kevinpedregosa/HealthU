PORT=4000
APP_BASE_URL=http://localhost:4000
APP_SESSION_SECRET=replace-with-a-long-random-secret

UCI_ISSUER=https://<uci-issuer>
UCI_AUTHORIZATION_ENDPOINT=https://<uci-issuer>/authorize
UCI_TOKEN_ENDPOINT=https://<uci-issuer>/token
UCI_JWKS_URI=https://<uci-issuer>/.well-known/jwks.json
UCI_CLIENT_ID=<uci-client-id>
UCI_CLIENT_SECRET=<uci-client-secret-if-required>
UCI_REDIRECT_URI=healthu://auth/callback
UCI_SCOPES=openid profile email

CALLBACK_SCHEME=healthu
REQUIRE_STUDENT_CLAIM=true
REQUIRE_MFA=true
